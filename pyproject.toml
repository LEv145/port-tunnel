[project]
name = "port-tunnel"
version = "0.1.0"
description = "Reverse TCP tunneling service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "src/client",
    "src/server",
    "src/common",
    "src/protocol",
]
