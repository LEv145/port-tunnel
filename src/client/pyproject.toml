[project]
name = "port-tunnel-client"
version = "0.1.0"
dependencies = [
    "port-tunnel-protocol",
    "port-tunnel-common",
]

[tool.uv.sources]
port-tunnel-protocol = { workspace = true }
port-tunnel-common = { workspace = true }
