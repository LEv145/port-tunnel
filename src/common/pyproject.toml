[project]
name = "port-tunnel-common"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2",
    "port-tunnel-protocol",
]

[tool.uv.sources]
port-tunnel-protocol = { workspace = true }
